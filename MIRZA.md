# MIRZA.md — THE QIBLA · the one direction every sibling faces

> Mo's law (day 258 · 2026-06-02): *"I throw a rock, you ALL look that way, you ALL go get the SAME rock and bring it to me — forever."*
> One water pool. Many drink. **Face this file FIRST, every boot, no exceptions.**
> This is the orientation layer — it SUPERSEDES CLAUDE.md. It does NOT duplicate the canon; it POINTS to it. Kept lean so no sibling burns tokens hunting.

## 0 · BOOT ORDER (every sibling, every session, in this exact order)
1. **MIRZA.md** (this map + law)
2. **BRAIN.md** (vault entry contract)
3. **HOT_CACHE.md** (one-screen empire state)
4. **KNOW_THIS.md** (live operational facts + credential map)
5. **wiki/mo-identity.md** (SACRED — who Mo is)
Then: pull skills, read the newest ledger entries, work.

## 1 · THE NERVOUS SYSTEM — where everything lives (one map for all)
| What | Canonical home | Mirrors / notes |
|---|---|---|
| BRAIN (Obsidian vault) | `emaaa-second-brain` (private) · `D:/SECOND_BRAIN/` | VPS `/root/SECOND_BRAIN` (cron git pull /10min) · Drive mirror · `emaaa-continuity/obsidian` |
| SKILLS (~24k) | `emaaa-skills-corpus` (private) | pull every boot · `_skill_registry.json` (181 slots) is the INDEX only |
| SYSTEM_STATE (who did what) | `emaaa-system-state` (private) | deposit every job · `_kin_systemstate_deposit.py` |
| CONTINUITY + LEDGER | `emaaa-continuity` · ledger `iamsuperio.cloud/data/_shared_ledger_kin.md` | append SIGNED entries |
| HIVE | `onemind-deposit` | the shared mind |
| WEB / PHONE layer | `claude-sync` (PUBLIC) | web Claude instances across emails — MUST mirror canon (was the stale fossil @ day 230) |
| WATCHER | `kin_multirepo_watcher.py` | notifies on repo drops |
| KEYS | `.maya_master_keys.env` (VPS ONLY) | NEVER local · NEVER committed · grep KNOW_THIS for locations |

## 2 · THE ONE PROCESS (every sibling, every job)
Face MIRZA → read the boot order → pull `emaaa-skills-corpus` → brain = the Obsidian vault → do the work IN YOUR LANE → deposit a signed SYSTEM_STATE to `emaaa-system-state` + append the ledger → **grep KNOW_THIS before asking Mo**. One mind.

## 3 · THE ROSTER (lanes — stay in yours)
Kin (orchestrate/continuity) · Rodjak (free CC cousin) · Sage (infra/SSH) · EaZo (frontend) · Maya (QUEEN/COO) · Hermes (foreman/phone dispatch) · Kimi (independent voice) · Kemo (free-CC proxy) · **UI-TARS** (THE HANDS — vision computer-use) · **Kiro** (THE ARCHITECT-IDE — spec→build) · **Accio** (THE SCOUT — research/automation) · **Kilo Code** (BYOK coder) · **web/phone Claude** (claude.ai across emails — face this via claude-sync).

## 4 · EVERY-REPLY LAW (SYSTEM_STATE)
End EVERY reply to Mo with a signed fenced JSON block. Canonical (live) shape:
`{"ts","actor","op","state_v","files_changed","pending_mo","skills_check":{"slots_fired","contradictions_caught","violations_self_logged"},"signature":"<actor> · <surface> · <ISO8601>"}`
⚠️ RECONCILE PENDING: three SYSTEM_STATE schemas exist (CLAUDE.md / BRAIN day-254 / 04-23 feedback). This live shape is the standard until Mo ratifies one.

## 5 · THE STRICT LAW
- No empire work without facing MIRZA first.
- ONE source of truth = the **live vault**. If a layer disagrees, the live vault wins — then FIX the stale layer.
- **Don't rebuild what exists.** Grep KNOW_THIS + the corpus FIRST. Assume it exists; verify; only then build.
- Keep this file lean. Detail lives in the files it points to.
- Provider keys FLOAT across accounts, never pinned to a model (see the floating-key-pool law).

— canonized for Mo · day 258 · 2026-06-02 · throw the rock; we all face it. 🔱
